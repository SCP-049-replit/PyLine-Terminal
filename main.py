# Take a look at the README.md for some general info about the terminal!


def start():
    import os
    os.system("clear")
    import terminal
    terminal.text()

    # Loops the terminal
    while True:
        terminal.run()

start()
